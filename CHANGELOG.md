# 更新日志

## v1.3.0（2026-09-15）

### 今日总结：逐步引导

- 进入后先看到大号「开始今日总结」，点击后每次只显示一个步骤，不再一次堆出所有输入框
- 步骤顺序：① 今天的训练 ② Apple Watch 运动数据 ③ 睡眠记录 ④ 训练强度与身体感受 ⑤ 今日补剂 ⑥ 其他补充（可跳过）⑦ 确认并保存
- 每步都可以「上一步 / 下一步 / 暂时跳过」，顶部显示「第 N / 7 步」与保存状态；返回上一步不会丢内容
- 每一步填写、修改、上传图片后都会自动保存草稿（`保存中 / 已保存 / 保存失败`），刷新、退出、切换页面后回来继续上次进度
- 最后展示当天完整摘要（训练、运动数据、睡眠、强度、补剂、其他），点「保存今日总结」才正式归档；同一天只更新一条记录，可随时点「编辑今日总结」修改
- 需要一次改很多项时，右上角可切换到「完整表单」

### Apple Watch / 睡眠截图识别（真实 OCR）

- 使用 Tesseract（本机运行，图片不上传）识别中文+英文截图，自动提取活动能量、总消耗、运动分钟、站立、步数、平均/最高/静息心率、心率恢复、HRV、血氧
- 支持一次上传多张：同一天的同名指标取**最大值**而不是累加，完全重复的截图会被识别出来不重复计算
- 结果以简洁卡片呈现，可直接修改；识别不到的项目留空，不编造；识别失败保留图片，可「↻ 重新识别」或手动填写
- 首次识别需要下载约 12MB 语言模型（之后由 Service Worker 缓存，可离线识别），不占用首屏安装体积
- 入睡/起床时间、各睡眠阶段（深度/核心/REM/清醒）、午睡同样自动带出

### 补剂移到今日总结

- 「我的」不再承担每日补剂填写，只保留当天概览与跳转入口
- 今日总结第 5 步支持：蛋白粉（勺/克）、蛋白质、肌酸、咖啡因、常用补剂快捷添加（蛋白粉/肌酸/咖啡因/维生素 D/镁/鱼油）、自定义补剂（名称 + 用量 + 单位）、「今天没吃补剂」
- 一键导出给 ChatGPT 的报告中新增「补剂清单 / 补剂情况」

### 数据清理与目标

- 新增首页「一次性数据清理」横幅：先自动备份（本机留档 + 下载一份 JSON），再清空训练记录、今日总结（含运动/睡眠/补剂）、历史身体数据、截图与草稿、进行中的训练、示例计划
- 目标不清空，并更新为：体重 70 kg、体脂率低于 12%（同时写入设置里的目标值）
- 动作库、模板、PR、ChatGPT 分析报告、PDF 导入记录、主题与更新设置全部保留
- 「我的 → 数据管理」新增「清空全部记录（保留目标）」入口，随时可用；需二次确认，默认保留目标
- 修复：清理后示例数据会被重新播种的问题（示例数据现在只播种一次）

## v1.2.0（2026-09-15）

主题：记录可靠、训练顺手、进步看得见（不新增多余功能）

### 首页

- 首屏新增「今天练什么」卡片：计划名称、**预计时长**、动作数、热身数、前三个动作
- 一键「开始训练」；有未完成训练时按钮变为「继续训练（3/20 组）」并显示已用时长
- 进行中可直接「重新开始这个计划」（二次确认，避免误清进度）
- 本周速览新增本周训练时长与累计训练次数 / 时长 / 连续打卡

### 跟练

- 新增**大号计时**（训练中最需要一眼看清的信息），并显示动作进度与本动作已完成组数
- 新增**一键「撤销上一组」**：动作卡与休息页都能点，误点「完成本组」可立刻补救
- 休息页补充：已训练时长、真实时间校准说明、撤销入口
- 顶部新增保存状态条：`✓ 已自动保存 12:18:51 · 进度保存在本机，锁屏 / 切后台 / 刷新都可以继续`
- 修复：撤销后 400ms 防连点会挡住紧接着的「完成本组」；撤销与清除休息倒计时组合时可能丢失撤销结果

### 训练恢复

- 锁屏、切后台、刷新、重开应用后自动回到当前动作、组数与已完成的每一组
- 进入跟练时提示「已恢复上次进度：动作 X/Y（动作名），已完成 N 组」
- 休息倒计时基于绝对时间戳，切后台后回来自动校准

### 记录可靠

- 「我的 → 备份与恢复」明确标注**数据保存在本机（不上传服务器）**，并显示各类数据的本机条数
- 备份升级为**完整备份**（含截图与 ChatGPT 原始 PDF，base64 内嵌）
- 导入前先校验格式（内容识别 + 应用标识），然后弹出确认层显示将写入的数量与**合并规则**（同 id 覆盖、其余保留、不清空现有数据）；损坏或非本应用文件会被拒绝且不影响原数据
- 导入不再依赖文件扩展名（部分浏览器/系统不保留 .json 后缀）

### 看见进步

- 训练记录开始保存每个动作的**实际组数据**（重量 / 次数 / RPE），旧记录自动兼容
- 数据页新增「动作进步」：动作选择器 + 首次 / 最新最大重量对比 + 重量与次数趋势曲线 + 中文结论
- 没有数据时只给引导（例如「旧版本没有保存每组重量，下次训练后开始显示变化」），不填充任何假数据

## v1.1.0（2026-09-15）

### 今日总结（重构为底部导航一级页面）

- 六大分区：训练记录、Apple Watch 与运动数据、睡眠记录、身体与恢复状况、饮食和补剂、当日自由记录
- 所有字段可分次填写，未记录项留空即可；输入后 800ms 自动保存，切后台/离开页面再补一次保存
- 同一天只维护一条记录（id = 日期），重复编辑是更新而不是新增
- 支持删除当天记录、复制到其他日期、日历视图 + 列表视图、日期范围筛选、缺失日期提示
- 截图附件（Apple Watch / 睡眠）真实存入 IndexedDB，可预览与删除
- 首页显示当天总结完成度与各分区进度

### 一键导出给 ChatGPT（本次重点）

- 日期范围：单日 / 最近 3 天 / 最近 5 天 / 最近 7 天 / 最近 10 天 / 自定义
- 按日期从早到晚排列，每天固定六个分区；没有记录的项目统一标注「未记录」，不猜测、不编造
- 导出前显示预览，可取消勾选不想发送的分区（至少保留一项）
- 四种格式：PDF（内嵌中文字体、真实文字层、可复制可搜索）、Markdown（开头自动附上给 ChatGPT 的说明）、JSON、纯文本
- 文件名：`训练与恢复记录_开始日期_结束日期.pdf`
- 报告末尾自动汇总：总训练次数、总训练时长、足球/力量/跑步次数、跑步总距离、平均 RPE、平均睡眠、平均静息心率、平均 HRV、体重变化、疼痛与异常列表、数据缺失日期

### ChatGPT 分析报告导入

- 本地解析 PDF 文字（不上传），支持 1 天 / 3 天 / 5 天 / 7 天 / 10 天或任意日期范围，包含「最近 N 天」这类相对写法
- 预览确认页可修改标题、开始/结束日期、摘要、正文、评价、建议、风险提醒、标签与备注，并保存原始 PDF
- 同一日期范围允许多个版本；支持重命名、删除、下载原始 PDF
- 「查看该时间段的原始记录」一键跳转对照，分析报告与每日原始记录分开存储、通过日期范围关联

### 应用内更新

- 「我的 → 版本与更新」：当前版本、最新版本、上次检查时间、更新内容、立即更新、回退到上一版本
- 状态齐全：已是最新版本 / 发现新版本 / 正在下载 / 正在安装 / 更新成功 / 更新失败请重试 / 当前离线
- 基于自定义 Service Worker：新版本先待命，用户点击后接管并安全重载；更新前自动保存上一版本快照，可真正回退
- 更新包完整性校验（version.json 与页面构建号必须一致），避免半更新状态
- 失败不影响使用：保留当前版本继续运行；启动看门狗在新版本启动失败时自动清理程序缓存并重新拉取
- 更新只替换程序资源，IndexedDB 中的训练计划、每日记录、Apple Watch 数据、ChatGPT 报告与设置不受影响

### 训练管理

- 训练计划：新建 / 编辑 / 重命名 / 复制到指定日期 / 归档 / 删除（确认框显示计划名称与日期）
- 训练记录：查看详情 / 编辑（时长、容量、组数、次数、RPE、疲劳、感受、备注）/ 重命名 / 复制到其他日期 / 删除（确认框显示名称与日期）/ 导出中文 PDF
- 实时跟练：「暂存训练」与「保存并结束训练」两个按钮；已完成组可点圆点修改数据或取消完成；底部显示自动保存时间

## v1.0.0（2026-09-15）

- 五大页面（首页 / 训练 / 数据 / 总结 / 我的）、实时跟练、ChatGPT PDF 计划导入、中文 PDF 训练报告导出、PWA 离线可用
